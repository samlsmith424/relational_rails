<h3>Machines at the Gym</h3>

<%= link_to "Create Machine", "/gyms/#{@gym.id}/machines/new" %>
<%= link_to "Sort Alphabetically", "/gyms/#{@gym.id}/machines?sort_by=name" %>

<% @machines.each do |machine| %>
  <%= link_to machine.name, "/machines/#{machine.id}" %>
<% end %>
